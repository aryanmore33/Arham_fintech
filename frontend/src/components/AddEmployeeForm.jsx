import { useState } from "react";
export default function AddEmployeeForm({ onSubmit }) {
  const [employee, setEmployee] = useState({ name: "", email: "", password: "" });
  const submit = async (event) => { event.preventDefault(); await onSubmit(employee); setEmployee({ name: "", email: "", password: "" }); };
  return <form onSubmit={submit} className="mb-5 grid gap-2 rounded-xl border border-cyan-100 bg-cyan-50 p-4 sm:grid-cols-4"><input required className="rounded-lg border p-2" placeholder="Employee name" value={employee.name} onChange={(e) => setEmployee({ ...employee, name: e.target.value })}/><input required className="rounded-lg border p-2" type="email" placeholder="Work email" value={employee.email} onChange={(e) => setEmployee({ ...employee, email: e.target.value })}/><input required minLength="6" pattern="(?=.*[A-Za-z])(?=.*\d)[A-Za-z\d]+" title="Use at least 6 letters and numbers" className="rounded-lg border p-2" type="password" placeholder="Temporary password" value={employee.password} onChange={(e) => setEmployee({ ...employee, password: e.target.value })}/><button className="rounded-lg bg-slate-900 px-4 py-2 font-semibold text-white">Add employee</button></form>;
}
