import { useState } from "react";
import { useAuth } from "../context/AuthContext";
export default function LoginPage() {
  const { login } = useAuth(); const [email, setEmail] = useState("manager@arham.com"), [password, setPassword] = useState("password123"), [error, setError] = useState("");
  const submit = async (event) => { event.preventDefault(); try { setError(""); await login({ email, password }); } catch (err) { setError(err.message); } };
  return <main className="grid min-h-screen place-items-center p-5"><div className="grid w-full max-w-5xl overflow-hidden rounded-3xl bg-white shadow-2xl shadow-cyan-950/40 md:grid-cols-2"><section className="hidden bg-gradient-to-br from-cyan-500 via-blue-600 to-slate-950 p-12 text-white md:block"><p className="text-sm font-bold tracking-[.25em] text-cyan-100">ARHAM FINTECH</p><h1 className="mt-12 text-4xl font-bold leading-tight">Brokerage intelligence,<br/>always current.</h1><p className="mt-6 max-w-sm text-cyan-100">Your portal reads a resilient local snapshot while exchange data synchronises securely in the background.</p><div className="mt-12 space-y-3 text-sm text-cyan-50"><p>✓ Role-based access</p><p>✓ Live BSE updates</p><p>✓ Protected incentive data</p></div></section><form onSubmit={submit} className="p-8 sm:p-12"><p className="text-sm font-bold tracking-widest text-cyan-600">SECURE SIGN IN</p><h2 className="mt-3 text-3xl font-bold text-slate-900">Welcome back</h2><p className="mt-2 text-sm text-slate-500">Use the account created by your manager.</p><label className="mt-8 block text-sm font-semibold text-slate-700">Work email<input className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 outline-none transition focus:border-cyan-500 focus:ring-4 focus:ring-cyan-100" value={email} onChange={(e) => setEmail(e.target.value)} placeholder="name@arham.com"/></label><label className="mt-4 block text-sm font-semibold text-slate-700">Password<input className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 p-3 outline-none transition focus:border-cyan-500 focus:ring-4 focus:ring-cyan-100" type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Enter your password"/></label>{error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button className="mt-6 w-full rounded-xl bg-slate-950 py-3 font-bold text-white transition hover:bg-cyan-600">Sign in securely</button></form></div></main>;
}
